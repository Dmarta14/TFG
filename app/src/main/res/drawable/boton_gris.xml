<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Estado cuando el botón está presionado -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#B0B0B0"/> <!-- Color más oscuro al presionar -->
            <corners android:radius="0dp"/> <!-- Bordes redondeados -->
            <stroke android:width="2dp" android:color="#808080"/> <!-- Borde gris oscuro -->
            <padding android:left="8dp" android:right="8dp"/>
        </shape>
    </item>

    <!-- Estado normal del botón -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#D3D3D3"/> <!-- Color normal -->
            <corners android:radius="0dp"/> <!-- Bordes redondeados -->
            <stroke android:width="2dp" android:color="#A9A9A9"/> <!-- Borde gris medio -->
            <padding android:left="8dp" android:right="8dp"/>
            <shadowColor android:color="#888888"/> <!-- Sombra -->
        </shape>
    </item>

</selector>